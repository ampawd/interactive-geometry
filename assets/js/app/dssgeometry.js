"use strict";

// TODO:

/**
 * @namespace DSSGeometry - the application global namespace
 * @description - everything is inside this namespace, all modules in the form {modulename/filename: exports}
 * example: {engine: {GeometryEngine : GeometryEngine}, math: {...}, shapes: {...} }
 * each module writes to this objects export stuff like classes (constructor functions), functions, constansts
 */
let DSSGeometry = {};